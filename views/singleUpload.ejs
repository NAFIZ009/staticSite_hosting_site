<div class="relative py-24 sm:px-6 sm:py-26 lg:px-8 mx-auto">
    <div
      class="absolute left-1/2 z-10 mt-5 flex w-screen max-w-max -translate-x-1/2 px-4 pb-20"
    >
      <div
        class="w-screen max-w-md flex-auto overflow-hidden rounded-3xl bg-white text-sm leading-6 shadow-lg ring-1 ring-gray-900/5"
      >
      
        <form action="/upload/single" method="post" id="form" class="<%= status && 'hidden' %>" enctype="multipart/form-data">
          <div class="p-4">
            <div class="mx-auto max-w-7xl px-2 lg:px-8 py-5">
              <div class="mx-auto max-w-2xl text-center">
                <h2 class="text-base font-semibold leading-7 text-indigo-600">
                  Deploy Your website
                </h2>
                <p
                  class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl"
                >
                  Deploy Your Static Website
                </p>
                <p class="mt-6 text-lg leading-8 text-gray-600">
                  Upload your <span class="font-bold">HTML File</span>.You can't add dedicated <span class="font-bold">CSS or JS Files</span> from here.For adding with HTML,CSS and JS you have to <span class="font-bold">Deploy Full Directory From <a class="underline text-gray-900" href="/upload">HERE</a></span>.The named
                  of your <span class="font-bold">HTML</span> file have to be <span class="font-bold">index.html</span>
                </p>
              </div>
            </div>
            <div>
              <label
                for="price"
                class="block text-sm font-medium leading-6 text-gray-900"
                >Project Name</label
              >
              <div class="relative mt-2 rounded-md shadow-sm">
                <input
                  type="text"
                  name="directory"
                  class="block w-full rounded-md border-0 py-1.5 pl-4 pr-20 text-gray-900 ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-gray-600 sm:text-sm sm:leading-6"
                  placeholder="Upload Your Directory"
                  id="fileInput"
                />
              </div>
            </div>

            <div>
              <label
                for="price"
                class="block text-sm font-medium leading-6 text-gray-900"
                >Upload Your File</label
              >
              <div class="relative mt-2 rounded-md shadow-sm">
                <input
                  type="file"
                  name="upload"
                  class="block w-full rounded-md border-0 py-1.5 pl-4 pr-20 text-gray-900 ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-gray-600 sm:text-sm sm:leading-6"
                  placeholder="Upload Your Directory"
                  id="fileInput"
                />
              </div>
            </div>
            <% if(err=='error'){ %>
            <div class="bg-red-100 mt-2 text-blue-700 px-4 py-3 rounded relative text-red-600 font-semibold" role="alert" id="alert">
              <strong class="">Error!</strong>
              <span class="block sm:inline" id="msg">Try After Some Time</span>
            </div>
            <% } %>
          </div>
          <p class="my-6 ms-4 text-lg leading-8 text-gray-600 font-semibold">
            <a href="/upload" class="">Want Deploy Full Directory?</a></a>
          </p>
          <div class="flex justify-center divide-x divide-gray-900/5 bg-gray-50">
            <button
              type="submit"
              class="flex items-center justify-center gap-x-1 p-3 font-semibold text-gray-900 hover:bg-gray-100 w-full"
              id="submitButton"
            >
              <i class="fa-solid fa-cloud-arrow-up flex-none text-gray-400"></i>
              <span class="font-bold">Upload</span>
            </button>
          </div>
        </form>
  
        <div class="p-12 text-center <%= !status && 'hidden' %>" id="url-box">
          <h2 class="text-3xl font-bold tracking-tight sm:text-4xl text-indigo-600">Your Site Is Deployed</h2>
          <span class="font-bold">Link:</span><br>
          <a class="text-xl font-bold tracking-tight text-gray-600 hover:underline" target="_blank" id="link" href="<%= siteURL %>">
            <%= siteURL %>
          </a>
        </div>
      
      </div>
    </div>
    <script>
      document.getElementById('form').addEventListener('submit',()=>{
        const subBtn=document.getElementById('submitButton');
        subBtn.setAttribute("disabled", "true");
        subBtn.innerHTML='<div class="w-8 h-8 border-t-4 border-blue-500 border-solid rounded-full animate-spin"></div>';
      })
      <% if( err=='error') { %>
      {
        const subBtn=document.getElementById('submitButton');
        subBtn.setAttribute("disabled", "true");
        subBtn.innerHTML='<i class="fa-solid fa-cloud-arrow-up flex-none text-gray-400"></i><span class="font-bold">Upload</span>';
      <% } %>
    </script>
  </div>
 